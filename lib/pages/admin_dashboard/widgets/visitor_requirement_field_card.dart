import 'package:flutter/material.dart';

class VisitorRequirementFieldCard extends StatelessWidget {
  const VisitorRequirementFieldCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}