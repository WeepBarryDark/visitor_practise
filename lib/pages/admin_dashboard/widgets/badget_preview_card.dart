import 'package:flutter/material.dart';

class BadgetPreviewCard extends StatelessWidget {
  const BadgetPreviewCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}